# Wrangler configuration for Cloudflare Workers
# Fill `account_id` or pass it via `--account-id` when deploying.
name = "kyouroga-bridge-git"
main = "src/worker.js"
compatibility_date = "2026-07-08"
type = "javascript"
workers_dev = true

# Optional: uncomment and set these if you want to publish to a zone/route
# account_id = ""
# zone_id = ""
# route = ""

[build]
command = ""

# No site/static assets configured - this is a Worker-only project
